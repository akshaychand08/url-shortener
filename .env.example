# Set to 'production' when deploying, 'development' for local work
NODE_ENV=development

# The base URL of your application.
# For local development, this should be http://localhost:<PORT>
# For Vercel deployment, this will be your production URL (e.g., https://shortly-app.vercel.app)
BASE_URL=http://localhost:3000

#----------------------------------------------------

# MongoDB Connection
# Get this from your MongoDB Atlas cluster.
# Replace <user>, <password>, and <cluster-url> with your credentials.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-url>/shortly?retryWrites=true&w=majority

#----------------------------------------------------

# JWT Authentication
# Use a long, random, and secret string for signing tokens.
# You can generate one using an online tool.
JWT_SECRET=your_super_secret_jwt_key_that_is_long_and_random

# How long a token should be valid for (e.g., 1d = 1 day, 7d = 7 days, 24h = 24 hours).
TOKEN_EXPIRES_IN=99999d

#----------------------------------------------------

# Admin User Seeding (used by the `npm run seed:admin` script)
# These credentials will be used to create the initial administrator account.
ADMIN_NAME=Admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_strong_admin_password

